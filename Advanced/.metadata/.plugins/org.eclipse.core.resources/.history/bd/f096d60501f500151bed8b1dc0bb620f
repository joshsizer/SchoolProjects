package main;

public class Book {
	private final String title;
	private final String author;
	private final String subject;
	private final long ISBN;
	private final int pages;
	
	/**
	 * The one and only constructor of a book object. After this, you cannot set any
	 * of this books properties. Once ya make a book, ya can't go changing it!
	 * 
	 * @param title The title of this book. This object will not capitalize the title for you.
	 * @param author The author of this book.
	 * @param subject The subject of this book, ie art, science, bridges.
	 * @param ISBN The international standard book number.
	 * @param pages
	 */
	public Book(String title, String author, String subject, long ISBN, int pages) throws NumberFormatException {
		if (String.valueOf(ISBN).length() != 10)
			throw new NumberFormatException("ISBN must be a 10 digit number");
		
		this.title = title;
		this.author = author;
		this.subject = subject;
		this.ISBN = ISBN;
		this.pages = pages;
	}

	public String getTitle() {
		return title;
	}

	public String getAuthor() {
		return author;
	}

	public String getSubject() {
		return subject;
	}

	public long getISBN() {
		return ISBN;
	}

	public int getPages() {
		return pages;
	}
	

}
